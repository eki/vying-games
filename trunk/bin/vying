#!/usr/bin/env ruby

require 'vying'
require 'vying/cli/cli'

if CLI::SUBCOMMANDS.include?( ARGV.first )

  command = ARGV.shift
  CLI.send( command )

else
  puts "blah blah blah, the --help output should be here..."

end

